{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "description": "A Node.js web and API server for inventory management without frameworks",
  "main": "webServer.js",
  "scripts": {
    "start": "concurrently \"npm run web\" \"npm run api\"",
    "web": "node web-server/webServer.js",
    "api": "node api-server/apiServer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
     "nodejs",
    "inventory",
    "api",
    "web-server",
    "no-framework"
  ],
  "author": "George Owoicho",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}